TS_pollkeyinput_onebyone()とTS_getch()の同時使用は非推奨(予期しない挙動を起こす可能性)

Implements: テスト対象の入力系への組み込み用。build.shでのビルドを推奨 入力キーのIDと待機時間を順番に読み込む通常のモードと、単純にchar変数として1文字ずつ読み込むモードの2種が指定できます。前者のモードを使用するときは
```
./build.sh
```

後者のモードを使用する場合、ビルド時に
```
./build.sh directRead
```
と記述してください